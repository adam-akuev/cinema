server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/adam-akuev/config-repo.git
          search-paths: movieservice, userservice, bookingservice
          default-label: main

encrypt:
  key: ${ENCRYPT_KEY}